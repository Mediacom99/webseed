services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: webseed
      POSTGRES_USER: webseed
      POSTGRES_PASSWORD: webseed
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
